generator client {
  provider = "prisma-client"
  output = "../src/generated/prisma"
}
datasource db {
  provider = "postgresql"
}
model Paciente {
  id Int @id @default(autoincrement())
  nome String
  email String
  telefone String

  @@map("pacientes")
}
